import BookListContainer from '../../containers/book-list-container/book-list-container.component';

interface HomeageProps {
}

export default function (props: HomeageProps) {
    return (
        <BookListContainer/>
    )
}