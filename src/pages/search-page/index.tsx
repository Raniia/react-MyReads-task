import SearchBookListContainer from '../../containers/search-container/search-container.component';

interface SearchProps {
}

export default function (props: SearchProps) {
    return (
        <SearchBookListContainer/>
    )
}